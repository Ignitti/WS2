{% comment %}
  Main List Collections Section
  Displays all available collections in a grid layout
{% endcomment %}

<div class="list-collections section-{{ section.id }}-padding">
  <div class="page-width">
    <div class="list-collections__header">
      {%- if section.settings.heading != blank -%}
        <h1 class="list-collections__title">{{ section.settings.heading }}</h1>
      {%- endif -%}
      
      {%- if section.settings.description != blank -%}
        <div class="list-collections__description">
          <p>{{ section.settings.description }}</p>
        </div>
      {%- endif -%}
    </div>

    {%- if collections.size > 0 -%}
      <div class="collections-grid">
        <ul class="grid grid--{{ section.settings.columns_desktop }}-col-desktop grid--{{ section.settings.columns_mobile }}-col-mobile" role="list">
          {%- for collection in collections limit: section.settings.collections_to_show -%}
            {%- unless collection.handle == 'frontpage' -%}
              <li class="grid__item">
                <div class="collection-card">
                  <a href="{{ collection.url }}" class="collection-card__link">
                    <div class="collection-card__image">
                      {%- if collection.image -%}
                        <img
                          src="{{ collection.image | image_url: width: 400 }}"
                          alt="{{ collection.image.alt | escape }}"
                          loading="lazy"
                          width="400"
                          height="400"
                        >
                      {%- elsif collection.products.first and collection.products.first.featured_media -%}
                        <img
                          src="{{ collection.products.first.featured_media | image_url: width: 400 }}"
                          alt="{{ collection.products.first.featured_media.alt | escape }}"
                          loading="lazy"
                          width="400"
                          height="400"
                        >
                      {%- else -%}
                        <div class="collection-card__placeholder">
                          <img
                            src="https://cdn.pixabay.com/photo/2017/02/05/12/31/candle-2042085_960_720.jpg"
                            alt="Collection placeholder"
                            loading="lazy"
                            width="400"
                            height="400"
                          >
                        </div>
                      {%- endif -%}
                      
                      <div class="collection-card__overlay">
                        <div class="collection-card__content">
                          <h2 class="collection-card__title">{{ collection.title }}</h2>
                          {%- if section.settings.show_collection_count -%}
                            <p class="collection-card__count">
                              {{ collection.products_count }} 
                              {%- if collection.products_count == 1 -%}
                                Product
                              {%- else -%}
                                Products
                              {%- endif -%}
                            </p>
                          {%- endif -%}
                          {%- if collection.description != blank -%}
                            <div class="collection-card__description">
                              {{ collection.description | strip_html | truncate: 100 }}
                            </div>
                          {%- endif -%}
                        </div>
                      </div>
                    </div>
                  </a>
                </div>
              </li>
            {%- endunless -%}
          {%- endfor -%}
        </ul>
      </div>
    {%- else -%}
      <div class="collections-empty">
        <div class="empty-state">
          <div class="empty-icon">🕯️</div>
          <h2>No Collections Found</h2>
          <p>We're currently setting up our collection pages. Check back soon for our curated candle collections.</p>
          <a href="/" class="button button--primary">Return Home</a>
        </div>
      </div>
    {%- endif -%}
  </div>
</div>

{% schema %}
{
  "name": "Collections list",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "default": "Collections",
      "label": "Heading"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Description"
    },
    {
      "type": "range",
      "id": "collections_to_show",
      "min": 2,
      "max": 20,
      "step": 1,
      "default": 12,
      "label": "Number of collections to show"
    },
    {
      "type": "range",
      "id": "columns_desktop",
      "min": 1,
      "max": 5,
      "step": 1,
      "default": 3,
      "label": "Number of columns on desktop"
    },
    {
      "type": "select",
      "id": "image_ratio",
      "options": [
        {
          "value": "adapt",
          "label": "Adapt to image"
        },
        {
          "value": "portrait",
          "label": "Portrait"
        },
        {
          "value": "square",
          "label": "Square"
        }
      ],
      "default": "square",
      "label": "Image ratio"
    },
    {
      "type": "checkbox",
      "id": "show_collection_count",
      "default": true,
      "label": "Show product count"
    },
    {
      "type": "select",
      "id": "columns_mobile",
      "default": "1",
      "label": "Number of columns on mobile",
      "options": [
        {
          "value": "1",
          "label": "1 column"
        },
        {
          "value": "2",
          "label": "2 columns"
        }
      ]
    },
    {
      "type": "header",
      "content": "Section padding"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Top padding",
      "default": 60
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Bottom padding",
      "default": 60
    }
  ]
}
{% endschema %}

<style>
.section-{{ section.id }}-padding {
  padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
  padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
}

@media screen and (min-width: 750px) {
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top }}px;
    padding-bottom: {{ section.settings.padding_bottom }}px;
  }
}

.list-collections {
  background: linear-gradient(135deg, rgba(26, 26, 26, 0.9), rgba(30, 58, 43, 0.7));
}

.list-collections__header {
  text-align: center;
  margin-bottom: 3rem;
}

.list-collections__title {
  font-family: 'Playfair Display', serif;
  font-size: 3rem;
  font-weight: 600;
  color: #D4AF37;
  margin-bottom: 1.5rem;
  letter-spacing: 0.05em;
}

@media screen and (max-width: 749px) {
  .list-collections__title {
    font-size: 2.5rem;
  }
}

.list-collections__description {
  font-family: 'Montserrat', sans-serif;
  font-size: 1.2rem;
  color: #cccccc;
  line-height: 1.6;
  max-width: 600px;
  margin: 0 auto;
}

.collections-grid {
  margin-top: 2rem;
}

.grid {
  display: grid;
  gap: 2rem;
  list-style: none;
  padding: 0;
  margin: 0;
}

.grid--1-col-desktop {
  grid-template-columns: 1fr;
}

.grid--2-col-desktop {
  grid-template-columns: repeat(2, 1fr);
}

.grid--3-col-desktop {
  grid-template-columns: repeat(3, 1fr);
}

.grid--4-col-desktop {
  grid-template-columns: repeat(4, 1fr);
}

.grid--5-col-desktop {
  grid-template-columns: repeat(5, 1fr);
}

@media screen and (max-width: 749px) {
  .grid--1-col-mobile {
    grid-template-columns: 1fr;
  }
  
  .grid--2-col-mobile {
    grid-template-columns: repeat(2, 1fr);
  }
  
  .grid {
    gap: 1rem;
  }
}

.collection-card {
  height: 100%;
  transition: transform 0.3s ease;
}

.collection-card:hover {
  transform: translateY(-5px);
}

.collection-card__link {
  display: block;
  text-decoration: none;
  color: inherit;
  height: 100%;
}

.collection-card__image {
  position: relative;
  aspect-ratio: 1;
  overflow: hidden;
  border-radius: 12px;
  background: rgba(30, 58, 43, 0.3);
}

.collection-card__image img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform 0.3s ease;
}

.collection-card:hover .collection-card__image img {
  transform: scale(1.05);
}

.collection-card__placeholder {
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
}

.collection-card__placeholder img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  opacity: 0.7;
}

.collection-card__overlay {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  background: linear-gradient(transparent, rgba(26, 26, 26, 0.9));
  padding: 2rem 1.5rem 1.5rem;
  transform: translateY(100%);
  transition: transform 0.3s ease;
}

.collection-card:hover .collection-card__overlay {
  transform: translateY(0);
}

.collection-card__content {
  text-align: center;
}

.collection-card__title {
  font-family: 'Playfair Display', serif;
  font-size: 1.5rem;
  font-weight: 600;
  color: #D4AF37;
  margin-bottom: 0.5rem;
  letter-spacing: 0.05em;
}

.collection-card__count {
  font-family: 'Montserrat', sans-serif;
  font-size: 0.9rem;
  color: #ffffff;
  font-weight: 500;
  margin-bottom: 0.75rem;
  text-transform: uppercase;
  letter-spacing: 0.1em;
}

.collection-card__description {
  font-family: 'Montserrat', sans-serif;
  font-size: 0.9rem;
  color: #cccccc;
  line-height: 1.4;
}

.collections-empty {
  text-align: center;
  padding: 4rem 2rem;
}

.empty-state {
  max-width: 500px;
  margin: 0 auto;
}

.empty-icon {
  font-size: 4rem;
  margin-bottom: 2rem;
}

.collections-empty h2 {
  font-family: 'Playfair Display', serif;
  font-size: 2rem;
  color: #D4AF37;
  margin-bottom: 1rem;
}

.collections-empty p {
  font-family: 'Montserrat', sans-serif;
  color: #cccccc;
  font-size: 1.1rem;
  margin-bottom: 2rem;
}

.button {
  display: inline-block;
  padding: 1rem 2rem;
  background: #D4AF37;
  color: #1a1a1a;
  text-decoration: none;
  border-radius: 8px;
  font-family: 'Montserrat', sans-serif;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  transition: all 0.3s ease;
}

.button:hover {
  background: #B8941F;
  transform: translateY(-2px);
}
</style>